import type { EntryPoint } from "repluggable";

export const MainViewPackage: EntryPoint[] = [{
    name: "Main-View",
    layer: "INFRA",
    
    declareAPIs() {
        return [];
    },

    attach(shell) {
        
    },

    extend(shell) {
        
    },
}]